export interface NavItem {
  label: string;
  href: string;
}

export interface GalleryItem {
  id: string;
  title: string;
  description: string;
  imageUrl: string;
  imageAlt: string;
}

export interface ReelItem {
  id: string;
  imageUrl: string;
  imageAlt: string;
}
